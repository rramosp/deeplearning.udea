# 4. ATTENTION IS ALL YOU NEED

 (3 semanas + 1.5 laboratorio)


**content** 

- R: Text sequences (tokenization, etc.)
- J: The attention model in RNNs (translation between languages, etc.)
- J: The transformer model (the layer, positional encodings, etc.)
- J: Transformer architectures (encoder, decoder-encoder, etc.)
- R: BERT /  training procedures (self supervised, then supervised)
- R: LLMs (gpt2, gpt3, …)
- R: Vision transformers (image tokenization, etc.)

**labs**

1. El mecanismo de atención (RNN vs KVQ en un transformer)
2. transformer architectures con texto.
3. vision transformers (con hugging face transformers library)
